/** @type {import('next').NextConfig} */
const nextConfig = {
    output: 'export',
    basePath: '/Site-Conselho-Tutelar-Nova-Ibia',
    images: {
        unoptimized: true
    }
};

export default nextConfig;
